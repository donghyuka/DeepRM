Installation
============

Prerequisites
-------------
* Python >= 3.9 (tested on 3.11)
* CUDA 11.x+ (for GPU inference)

.. code-block:: bash

   git clone <your‑fork>
   cd deeprm
   python -m pip install -r requirements.txt